<!DOCTYPE html>
<html>
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
      <h1><%= flair.name %></h1>
      <p color="<%= flair.color %>"><%= flair.color %></p>
      <form style="display:initial" action="/topics/:topicId/posts/<%= flair.postId %>/flairs/<%= flair.id %>/destroy" method="post">
        <button type="submit" class="btn btn-danger">Delete flair</button>
      </form>
      <a href="/topics/:topicId/posts/<%= flair.postId %>/flairs/<%= flair.id %>/edit" class="btn btn-warning">Edit Flair</a>
    </main>

    <% include ../static/partials/baseScripts.ejs %>
  </body>
</html>
